# 数据链总览

> 最后更新: 2026-07-21 (仓库16类数据 + ML接入)
> 数据链 = 外部 API → 数据仓库 → 信号文件 → 跨系统注入 → 融合决策

---

## 一、数据流全景

```
                ┌──────────────────────────────────────────────────────┐
                │                 外部数据源                             │
                │  pytdx(TCP)  Tushare(HTTP)  Baostock(TCP)  EM/Sina   │
                └──────────┬──────────────────────────┬────────────────┘
                           │                          │
                           ▼                          ▼
                ┌──────────────────┐      ┌──────────────────────┐
                │  services/       │      │  子系统独立获取路径    │
                │  data_warehouse/ │      │  (LY直连Sina、AT直连  │
                │  (统一缓存+限流)  │      │   akshare、ML直连EM)  │
                └────────┬─────────┘      └──────────┬───────────┘
                         │                           │
                         ▼                           ▼
                ┌───────────────────────────────────────────────┐
                │          信号文件 (data/realtime/)              │
                │  ly_signal.json  ml_signal.json  at_signal.json│
                └──────────────────────┬────────────────────────┘
                                       │
                                       ▼
                ┌───────────────────────────────────────────────┐
                │           跨系统数据注入 (LLM prompts)          │
                │  ML因子→AT context (LY注入已停止2026-07-22)   │
                └──────────────────────┬────────────────────────┘
                                       │
                                       ▼
                ┌───────────────────────────────────────────────┐
                │             融合引擎 (fusion_engine.py)         │
                │  三系统加权 → L7决策 → 资金分歧检测 → 输出     │
                └──────────┬────────────────────────────────────┘
                           │
                           ▼
                ┌───────────────────────────────────────────────┐
                │   推送 (push/) → 回测 (testing/) ← 记录      │
                └───────────────────────────────────────────────┘
```

## 二、各子系统数据链

### LY 系统（量化信号）
```
Sina HTTP → fetch_daily_bars() → parquet缓存 → RandomForest预测 → ly_signal.json
Tushare (备用) → 数据仓库缓存 (可选路径)
```

### ML 系统（AI分析）
```
首选:  pytdx(TCP) → Tushare → Baostock → EM(末位)
行情:  Tencent(实时) → Sina(实时) → akshare_em
财务:  Tushare(已配Token) → Baostock TCP → akshare_em(末位)
新闻:  EM(盘前缓存) → CNINFO巨潮(公告)
资金流: Tushare → EM(盘前缓存)
```

### AT 系统（多智能体辩论）
```
OHLCV:  akshare(EM) → pytdx(TCP) → baostock
财务:   akshare(EM) → 同花顺(ths)
三表:   同花顺(ths,非EM)
资金流: akshare(EM)独有
新闻:   akshare(EM) → stock_info_news
```

## 三、数据链路质量指标

| 维度 | 衡量方式 | 当前状态 |
|------|---------|---------|
| 数据覆盖 | 仓库数据类型数 | 16 类 ✅ |
| 缓存命中率 | 首次调用后命中率 | ~100% ✅ |
| 数据新鲜度 | 各数据 TTL 是否遵守 | 5min-24h TTL ✅ |
| 降级链路 | 首选源失败后降级成功率 | 5层降级链已验证 ✅ |
| ML 接入 | ML 子系统仓库优先 | ✅ 2026-07-21 |
| AT 接入 | AT 子系统仓库优先 | ✅ 2026-07-21 |

## 四、相关文档

| 文档 | 说明 |
|------|------|
| `data-sources.md` | 数据源配置与依赖审计 |
| `data-warehouse.md` | 数据仓库服务层实现（推荐阅读） |
| `data-warehouse-pattern.md` | 数据仓库架构模式文章 |
| `llm/injection.md` | LY/ML 信号注入 AT 提示词 |
| `architecture/system-overview.md` | 系统架构与运行时序 |
